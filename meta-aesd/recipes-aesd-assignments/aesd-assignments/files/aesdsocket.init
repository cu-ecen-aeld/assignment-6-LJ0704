#!/bin/sh

case "$1" in
  start)
        echo "Starting aesdsocket"
        /usr/bin/aesdsocket -d
        ;;
  stop)
        echo "Stopping aesdsocket"
        killall aesdsocket
        ;;
  restart)
        killall aesdsocket
        /usr/bin/aesdsocket -d
        ;;
  *)
        echo "Usage: $0 {start|stop|restart}"
        exit 1
esac

exit 0
